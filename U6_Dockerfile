# U6 — Dockerfile template for sparse_regvelo validation environment
# STATUS: template only; no digest; no external-rerun claim.
# AUTHORIZATION: generated as skeleton; must be reviewed and built by human.
# NOTE: dependency versions and base image must be verified before use.

FROM python:3.12-slim

# System dependencies (template placeholders)
RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# Python dependencies (template placeholders — versions must be provided by human)
COPY requirements.txt /tmp/requirements.txt
RUN pip install --no-cache-dir -r /tmp/requirements.txt

# Copy engine and validation code
COPY sparse_regvelo/ /opt/sparse_regvelo/
COPY validation_Sparse_regvelo/ /opt/validation_Sparse_regvelo/

WORKDIR /opt/validation_Sparse_regvelo

# Default: run unit tests only (no data download, no full-atlas run)
CMD ["pytest", "-q", "/opt/sparse_regvelo/tests/"]

# 1. Pin exact package versions in requirements.txt (torch, numpy, scipy, scikit-learn, anndata, scanpy, etc.).
# 2. Add conda/mamba alternative if CUDA runtime is required.
# 3. Add volume mounts for /mnt/t9/datasets as read-only.
# 4. Build image and record digest only after successful validation run.
